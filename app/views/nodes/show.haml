%p
- @node.parents.each do |parent|
  %a{ :title => parent.label, :href => url_for( parent ) }
    = parent.info
  != '&nbsp;'
%br
|
%br
v
%br
%h3{ :title => @node.label }
  = @node.info
|
%br
v
%br
- @node.children.each do |child|
  %a{ :title => child.label, :href => url_for( child ) }
    = child.info
  != '&nbsp;'
%br
